# CSCI 1030U - Group Project: Milestone Requirements

This is the single reference for the term-long group project: what you build, what each
milestone must contain, how each member's contribution is tracked, and how it is all
marked. Read it once at Milestone 0, then come back to the milestone you are working on.

**Do not edit this file.** It is the requirements, the same for every team. `README.md` is
the file you make your own.

**The files you fill in and keep up to date:**

| File | What it is | Updated |
|------|------------|---------|
| [`README.md`](README.md) | the way in - the application, the **team table** (full names and GitHub usernames), and the tech plan; then your project's own README | Milestone 0, then Milestone 1 on |
| [`CONTRIBUTIONS.md`](CONTRIBUTIONS.md) | who owns which slice, and the commit that proves each member did each topic | every milestone |
| [`AI-USAGE.md`](AI-USAGE.md) | the AI policy, and your team's disclosure of what AI you used | every milestone |

---

## Contents

- [Purpose](#purpose)
- [The project](#the-project)
- [Teams and participation](#teams-and-participation)
- [Milestone 0 - Team, Topic, and Repository](#milestone-0---team-topic-and-repository)
- [Milestone 1 - Core Prototype (Week 4)](#milestone-1---core-prototype-week-4)
- [Milestone 2 - Practical Application (Week 8)](#milestone-2---practical-application-week-8)
- [Milestone 3 - Final Product (Week 12)](#milestone-3---final-product-week-12)
- [The checkpoint sessions](#the-checkpoint-sessions)
- [Git workflow](#git-workflow)
- [Assessment](#assessment)
- [Topic coverage map](#topic-coverage-map)
- [Suggested projects](#suggested-projects)
- [Getting help](#getting-help)

---

## Purpose

The group project is the term-long, hands-on counterpart to the lectures and labs. It is
the primary vehicle for two learning outcomes that a written exam cannot assess:

- **Communicate** technical designs clearly (oral checkpoints + written documentation).
- **Collaborate** in a team to plan, build, and deliver software on schedule using GitHub.

It is deliberately designed so that, by the end of the term, your repository **exercises
as much of the course as possible** - every unit, from problem-solving through
intermediate programming, has to show up in the finished product.

Two design rules drive everything below:

1. **Each milestone demonstrates the topics from the lectures that came before it.** You
   are never asked to use something you haven't been taught.
2. **Every student must personally contribute each topic.** For each milestone, every team
   member must have at least one *non-trivial commit* (see
   [What counts as a non-trivial commit](#what-counts-as-a-non-trivial-commit)) for each
   listed topic. Participation is individual and traceable in the git history.

---

## The project

**Theme: a networked, multi-user Python application** - by default, a **turn-based
multiplayer game with an AI opponent and persistent data** (leaderboard, save/resume).

The networked, multi-user theme is not arbitrary: it is the smallest theme that forces the
whole course to appear. A finished project has to talk over sockets (Unit 05), serve
several users at once (async / threads, Unit 05), organize itself with classes (OOP, Unit
03), read and write files (Unit 03), present an interface (Unit 03), and make good use of
a data structure and an algorithm (Unit 04) - all built on Unit 02 fundamentals and
planned with Unit 01 techniques.

Your team chooses its specific application, subject to instructor approval at Milestone 0.
See [Suggested projects](#suggested-projects) for ideas that are known to fit.

### Scope and tech constraints

- **Python standard library only**, so the project stays aligned with what is taught:
  `socket`, `asyncio` / `threading` / `multiprocessing`, `tkinter`, `argparse`, `json`,
  `csv`. Any additional dependency needs instructor approval - ask for it in the tech
  plan in your `README.md`.
- **Team size: 4-5 students.** The plan scales across that range; the per-student topic
  rule is the same regardless of size.
- Keep the scope honest: a small application that cleanly hits every topic beats an
  ambitious one that fakes half of them.

---

## Teams and participation

### The "everyone contributes" model

The project is decomposed into **vertical slices** - one comparable feature per team
member, each built end to end. When a milestone adds a topic, *each* member adds that
topic *to their own slice*. For example, at the networking milestone, no team writes one
server that one person owns; instead each member adds the message type and handler for
their own feature to the shared server. That way a single shared codebase still yields an
individual, non-trivial commit per topic per person.

### The team table (required at Milestone 0)

The table at the top of [`README.md`](README.md) lists every member's full name and
**GitHub username**, and nothing else. It is what lets commits in the history be matched to
people, so the usernames have to be exactly right - the ones that actually author your
commits.

### The slices and topic matrix (required every milestone)

[`CONTRIBUTIONS.md`](CONTRIBUTIONS.md) is where each member's work is recorded and proved.
It opens with the **slices** table - who owns which feature, agreed at Milestone 0 - and is
then a table per milestone, mapping each student to the commit(s) or pull request(s) that
demonstrate each required topic for that milestone.

| Student | Topic A | Topic B | Topic C | ... |
|---------|---------|---------|---------|-----|
| Alice   | #12     | #15     | abc1234 | ... |
| Bob     | #13     | #16     | def5678 | ... |

Cells hold a merged pull request number or a commit hash. At each checkpoint, every
student speaks to their own row. **A blank cell means that student did not demonstrate
that topic, and loses those individual marks - the rest of the team is unaffected.**

Keep all three milestone tables in the one file. Do not delete earlier milestones; the
history matters.

### What counts as a non-trivial commit

A commit (or small set of commits) **authored by that student** that adds real, working
functionality for the topic. As a guideline:

- implements a genuine piece of logic - roughly a complete function, class, handler, or
  screen (on the order of 15+ meaningful lines), not a one-liner;
- lands on the project's `main` branch (*how* it gets there is the
  [git workflow](#git-workflow), whose emphasis grows over the term);
- is **not** formatting, renaming, comments, config, or generated/boilerplate code.

The TA verifies against git author information and the matrix. "I pair-programmed it" is
fine, but the commit must be authored - or co-authored via a `Co-authored-by:` trailer -
by the student claiming it.

---

## Milestone 0 - Team, Topic, and Repository

**Due:** between Lab 01 and Lab 02. **Walked through in Lab 01.** Not graded for code.

- **Form the team** (4-5 students).
- **Brainstorm and choose a topic** - the application you will build, within the
  networked-app theme (see [Suggested projects](#suggested-projects)).
- **Accept the invitation to the CSCI1030U organization** on GitHub. It is sent out after
  the Lab 01 quiz closes, using the usernames submitted there. If it hasn't arrived a day
  or two after the quiz closes, tell your instructor - usually the username had a typo.
- **Create this shared repository from the project template**: **one** member opens the
  group-project template link on Canvas, clicks **Use this template**, and sets
  - **Owner:** `CSCI1030U` (the organization, *not* their own account),
  - **Repository name:** `project-yourteamname`,
  - **Visibility:** **Private**.

  That member then goes to **Settings -> Collaborators and teams** and adds **every other
  member** with **Write** access, plus the instructor and your TA. Everyone else clones it.

  > Use **Use this template**, not **Fork**. A fork cannot be made private and stays wired
  > back to the template.

- **Fill in [`README.md`](README.md)** - this is your Milestone 0 write-up, and it has
  three parts:
  - **the application** - what you are building;
  - **the team table** - every member's full name and GitHub username;
  - **the tech plan** - standard library only, or a request to use something else.

  Commit and push it, and have at least a couple of members make a commit, so everyone is
  set up and authenticated.
- **Fill in the slices table in [`CONTRIBUTIONS.md`](CONTRIBUTIONS.md)** - the one vertical
  slice each member owns for the term.
- **Read [`AI-USAGE.md`](AI-USAGE.md)** together and agree on how your team will work.
- **Agree on your git workflow** (see [Git workflow](#git-workflow)): commit in your own
  name and push; grow into branches and pull requests as the project goes on.

### Definition of done

- The private repository exists in the `CSCI1030U` organization with every member added.
- `README.md` is filled in and pushed: the application, the team table (every member's
  full name and GitHub username), and the tech plan.
- The slices table in `CONTRIBUTIONS.md` names the feature each member owns.

---

## Milestone 1 - Core Prototype (Week 4)

**Units 01-02.** Built over Weeks 2-4. **Presented in Lab 04.**

A **single-machine, text-only prototype**: the core logic of your application working
locally in the terminal. **No networking, no GUI, no files yet** - those come later. This
is where you prove the idea actually runs.

It only uses what has been taught by Week 4: Units 01-02 (problem-solving, variables,
types, conditionals, loops, strings, lists, dictionaries, functions).

### Per-student topics

Keep the vertical-slice model from your Milestone 0 slices table. Every member must have at least one
[non-trivial commit](#what-counts-as-a-non-trivial-commit) for **each** of these four
topics, **in their own slice**:

- [ ] **Planning / problem-solving** (Unit 01) - a short written spec/decomposition for
      your slice: what it does, the steps, the data it needs. Put it in the repo (a
      section of the README, or a short note file of your own) and commit it.
- [ ] **Control flow** (Unit 02) - conditionals and loops that drive your feature.
- [ ] **Collections** (Unit 02) - lists and/or dictionaries holding your feature's state.
- [ ] **Functions** (Unit 02) - at least one non-trivial function you wrote yourself.

One member's slice might be the board/state, another's the input loop, another's the
scoring, another's a simple computer player - but **each** slice should show all four
topics. If a topic doesn't naturally fit a slice, talk to your lab instructor early; don't
leave a cell blank.

### Definition of done

- `python main.py` starts a working version of your core loop that a person can actually
  play/use in the terminal.
- `README.md` has been rewritten as **your project's** README - what the app is and how to
  run it - keeping the team table and the pointers to the other files.
- The **Milestone 1** table in `CONTRIBUTIONS.md` is filled in, every member's row
  complete.
- The **Milestone 1** section of `AI-USAGE.md` is filled in.

---

## Milestone 2 - Practical Application (Week 8)

**Unit 03.** Built over Weeks 5-8. **Presented in Lab 08.**

The prototype becomes a **real, single-machine application**: organized into classes, with
a proper interface, persistence, and configuration. Still single-machine - networking is
Milestone 3. It lands right after the Week 7 midterm, so it consolidates the whole
practical-programming unit.

### Per-student topics

Keep your vertical slice. Every member must have at least one
[non-trivial commit](#what-counts-as-a-non-trivial-commit) for **each** of these five
topics, **in their own slice**:

- [ ] **OOP** (Unit 03) - at least one **class you authored**, with methods and state.
- [ ] **File I/O** (Unit 03) - a save/load, high-score, or config feature that reads
      and/or writes a file (`json` or `csv`). Serializing your own class (a
      `to_dict`/`from_dict`) into the shared save file counts.
- [ ] **Recursion** (Unit 03) - a recursive function used somewhere real in your slice.
- [ ] **Command-line arguments** (Unit 03) - an `argparse` option you added (difficulty,
      board size, data file, ...).
- [ ] **Interface** (Unit 03) - a TUI menu/screen or a `tkinter` element you built.

If a topic is hard to fit into your slice, talk to your lab instructor early - don't leave
a cell blank.

### Definition of done

- The app is **class-based** - the core entities are modelled as classes with methods and
  state.
- It launches with useful **`argparse`** options.
- It **saves and loads** state to disk (save/resume and/or a high-score file, in `json` or
  `csv`) and the saved state survives a restart.
- It is usable through a **TUI menu** or a **`tkinter` GUI**.
- The **Milestone 2** table in `CONTRIBUTIONS.md` is filled in.
- The **Milestone 2** section of `AI-USAGE.md` is filled in.
- The `README.md` still matches what the app actually does.

---

## Milestone 3 - Final Product (Week 12)

**Units 04-05.** Built over Weeks 9-12. **Presented in Lab 12.**

The application becomes **networked and multi-user**, backed by a real data structure and
a real algorithm. This is the finished product and the final presentation.

### Per-student topics

Keep your vertical slice. Every member must have at least one
[non-trivial commit](#what-counts-as-a-non-trivial-commit) for **each** of these four
topics, **in their own slice**:

- [ ] **Data structure** (Unit 04) - a meaningful use of a stack, queue, or BST (turn
      queue, move-history/undo stack, sorted leaderboard, ...).
- [ ] **Algorithm strategy** (Unit 04) - a feature using backtracking, divide-and-conquer,
      greedy, or dynamic programming (an AI opponent, pathfinding, scoring/optimization),
      with a **short Big-O note** in your `README.md`.
- [ ] **Sockets** (Unit 05) - a client-server message + handler for your feature.
- [ ] **Concurrency** (Unit 05) - an async task, thread, or process that lets your
      feature's I/O run alongside others (e.g. your handler on the concurrent server).

### Definition of done

- **Two or more clients** interact through your **server at the same time** (sockets).
- The server (or clients) use **concurrency** - async tasks, threads, or processes - so
  multiple users are served without blocking each other.
- A meaningful **data structure** is used somewhere real.
- An **algorithm strategy** drives a feature, with a **Big-O note** for it.
- The product is packaged with a **`README.md`** (what it is + run instructions), the
  complete **`CONTRIBUTIONS.md`** covering all three milestones, a complete
  **`AI-USAGE.md`**, and a **Design** section in the `README.md` - about a page - covering
  the architecture and the Big-O analysis.

---

## The checkpoint sessions

Each checkpoint lab (Weeks 4, 8, 12) runs the same way.

1. **Submit before the session.** The work is built by your team over the weeks leading up
   to it and must be pushed to this repository **before the lab begins**. What is in your
   repository at the **start of the lab** is what gets assessed.
2. **The TA checks each team.** At the start of the session the lab instructor goes around
   the teams and confirms each submission matches the milestone's definition of done, with
   `CONTRIBUTIONS.md` and `AI-USAGE.md` up to date.
3. **Each team presents (8-10 minutes).** **Every member must speak.**

### What the presentation covers

- an **introduction to the team** - who's in the group, which slice each member owns;
- **background on the topic** - what the application is, for a first-time viewer;
- the **technical concepts implemented**, with **each member speaking to their own row**
  of `CONTRIBUTIONS.md` and the code behind it;
- a **demonstration** of the project in action.

**The balance shifts across milestones.** You introduce yourselves and your topic at
Milestone 1, so at Milestones 2 and 3 you **compress** the introduction and background to
a quick reminder and **spend more of the time** on the technical explanation and the demo,
which grow substantially at each milestone. At Milestone 3 the technical walkthrough and a
live multi-client demo carry the presentation, and you close with a short reflection: what
you would do differently, and what you are proud of.

Presentations carry most of the project's oral-communication assessment.

---

## Git workflow

Good git habits are part of the teamwork learning outcome, but they are introduced gently
so first-semester students aren't blocked by tooling. The **emphasis grows over the term**,
and it is a *recommended* progression, not a pass/fail gate - participation is always
graded on each student's own identifiable commits, whichever workflow your team uses.

- **Lab 01 through Milestone 1 (getting started):** everyone can clone, commit with a
  meaningful message **in their own name**, and push. Pull requests are taught in Lab 01
  and encouraged, but at Milestone 1 all that's needed is that each student has their own
  commits on `main`.
- **Milestones 2 and 3 (the recommended way to work):** move to a **branch per feature**,
  merged to `main` through a **pull request a teammate reviews**. This is the recommended
  workflow and makes the strongest case for the teamwork outcome - but a team that keeps
  committing straight to `main` is not penalized for that alone, as long as each member's
  contributions are clear in the history.

Throughout:

- Commit messages are meaningful and in the author's name (use a `Co-authored-by:` trailer
  for genuine pairing).
- `main` always runs. Protect it if your team is comfortable doing so.

---

## Assessment

Each milestone is graded on four fronts. The full rubric for each checkpoint is attached
to the assignment in Canvas.

1. **Functionality** - does the deliverable meet its definition of done?
2. **Topic coverage** - are all listed topics present and used meaningfully, rather than
   token or decorative?
3. **Individual participation** - does every member have their required non-trivial
   commits, verified via git history and the topic matrix? **(Scored per student.)**
4. **Presentation and documentation** - clear demo, clear speaking, an up-to-date README
   including its Milestone 3 design section.

Fronts 1, 2, and 4 are largely team scores; front 3 is individual - so a strong team does
not carry a member who did not contribute, and one quiet slice does not sink the team.

---

## Topic coverage map

How the project is intended to touch the whole course:

| Unit | Topic | Where it appears | Milestone |
|------|-------|------------------|-----------|
| 01 | Problem-solving, decomposition | Milestone 0 write-up + per-slice specs | 0, 1 |
| 02 | Variables, types, conditionals, loops | Core game/app loop | 1 |
| 02 | Strings, lists, dictionaries | Game/app state | 1 |
| 02 | Functions | Everywhere | 1 |
| 03 | OOP | Classes for entities/players/board | 2 |
| 03 | File I/O | Save/load, high scores, config | 2 |
| 03 | Recursion | AI search, board fill, tree walk | 2 |
| 03 | Command-line arguments | Launch/config options | 2 |
| 03 | TUI / GUI | The client interface | 2 |
| 04 | Data structures (stack/queue/BST) | Turns, history, leaderboard | 3 |
| 04 | Algorithm strategies + Big-O | AI / pathfinding / scoring + README design note | 3 |
| 05 | Sockets | Client-server messages | 3 |
| 05 | Async / threads / processes | Concurrent server for many users | 3 |

---

## Suggested projects

Any of these can hit every required topic; the notes show the natural fit for the harder
Unit 04-05 items.

1. **Networked board game** (Battleship, Connect Four, Checkers) - AI opponent
   (backtracking / minimax = divide-and-conquer), move-history stack, sorted leaderboard,
   concurrent server for multiple matches.
2. **Multiplayer trivia / quiz** - question bank from file, real-time scoreboard (BST or
   sorting), greedy/DP scoring, async server fanning questions to many clients.
3. **Multiplayer word game** (Wordle-battle, Boggle, Scrabble-lite) - dictionary loaded
   into a fast lookup structure, a solver/AI (backtracking), networked rounds.
4. **Card game** (Blackjack, Crazy Eights, a Uno-like) - deck/hand classes, turn queue,
   simple AI (greedy), save/resume, concurrent tables.
5. **Collaborative tool** (shared to-do, whiteboard, or chat-with-games) - command
   parsing, persistence, concurrent clients, a data structure for the shared state.

---

## Getting help

A lab instructor is present for every lab session, not just the checkpoint weeks. Use the
weeks *leading up* to a checkpoint to unstick your team early - especially on git, and on
making sure every member has a real commit for each topic. Don't wait until presentation
day to discover a slice is missing. Networking and concurrency (Milestone 3) are the
trickiest topics of the term; get two clients talking to your server well before Week 12.
